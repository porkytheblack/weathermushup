/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "i.scdn.co",
      "images.unsplash.com"
    ]
  }
}

module.exports = nextConfig
